begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "big_beta"
    gem.summary = "Rails engine for handling beta signups and invites."
    gem.email = "tony+bigbeta@tonystubblebine.com"
    gem.authors = ["Tony Stubblebine"]
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
  end
rescue
  puts "Jeweler or dependency not available."
end
